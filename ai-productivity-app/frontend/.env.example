# Frontend Environment Configuration
# Copy this to .env.local for local development
# Or set these in your deployment environment

# API Configuration
# For production, set this to your API URL (without trailing slash)
# Leave empty to use relative URLs (recommended for production behind reverse proxy)
VITE_API_URL=

# WebSocket Configuration
# For production with a separate WebSocket endpoint, set this explicitly
# If not set, it will use the same host as the frontend (recommended)
# Example: wss://lakefrontdigital.io
VITE_WS_URL=

# For local development with backend on different port:
# VITE_API_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000

# For production behind NGINX reverse proxy (recommended):
# Leave both empty - the app will use relative URLs and the current host

# Cookie Configuration
# Set to true only for local development over HTTP
# Should be false (or omitted) for production HTTPS
INSECURE_COOKIES=false

# Development Configuration
# Uncomment for local development
# VITE_DEV_MODE=true
