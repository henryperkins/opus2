# Frontend Production Environment Configuration
# Use relative URLs; NGINX reverse proxy routes requests to the backend

VITE_API_URL=
VITE_WS_URL=

# Cookie Configuration (must be false for HTTPS)
INSECURE_COOKIES=false
