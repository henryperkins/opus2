# Vector Store Configuration - defaults to SQLite VSS for development
VECTOR_STORE_TYPE=sqlite_vss

# Set to 'qdrant' if you want to use Qdrant instead
# VECTOR_STORE_TYPE=qdrant
# QDRANT_HOST=localhost
# QDRANT_PORT=6333

# Tree-sitter configuration
# These packages are automatically detected and used if available:
# - tree-sitter-language-pack (preferred)
# - Individual packages: tree-sitter-python, tree-sitter-javascript, etc.
# - Compiled shared library: build/languages.so (fallback)

# If none are available, parsing will gracefully fall back to basic text processing
